# SW1 post-freeze supplement v0.3 — Signed winding

SW1 remains a supplemental reversal check with its own record. It does not retroactively replace a frozen primary score.

Use conserved signed integer W in the canonical source-free ensemble. Define \(q_\alpha=W_\alpha\bmod2\), canonical nonnegative residues, and \(\operatorname{sgn}(0)=0\). Under a current-reversal-invariant equilibrium measure,
\[
S_{\rm odd,\alpha}=\mathbb E[q_\alpha\operatorname{sgn}W_\alpha]=0.
\]
Reversal takes W to −W while keeping q unchanged, so the integrand changes sign. With \(f_{\rm odd}=\mathbb E q_\alpha\),
\[
f_{\rm odd}^{+}=(f_{\rm odd}+S_{\rm odd})/2,\qquad
f_{\rm odd}^{-}=(f_{\rm odd}-S_{\rm odd})/2.
\]
Odd winding is never zero, which makes this split exhaustive.

The reversal proof concerns the target measure. Proposal symmetry pairs \((I,\varepsilon)\) with \((-I,-\varepsilon)\); it does not require equal acceptance for opposite moves from the same fixed I. A production record must test the actual kernel and retention schedule as needed. The reference tests only algebraic state validity under reversal, not an equilibrium SW1 estimate.

Retain the source alert labels: `NO_ALERT`, `ALERT_UNEXPLAINED`, `STATISTICAL_OR_PHYSICAL`, `SHARED_CHAIN_DEFECT_CONFIRMED`, `SW1_UNRESOLVED`. Unexplained required alerts withhold the joint interpretation; a confirmed shared-chain validity defect makes affected scientific inference invalid while preserving the historical numerical score.

Freeze axes, time address, samples, uncertainty construction, multiplicity and thresholds before target output. Preserve signed events and distinguish full-state mixing from parity visits. A zero aggregate \(\eta\) does not imply no winding motion. Sourceful averaged current is not an admissible substitute for W in this supplement.

Current status: physical SW1 `NOT_RUN`; exact reversal fixture checked. See [the original signed-winding supplement](sources/13_SW1_Post_Freeze_v0.2_Signed_Winding_Cocycle.md).
